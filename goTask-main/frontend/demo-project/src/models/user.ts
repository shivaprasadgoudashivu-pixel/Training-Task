export interface User  {
  name: string;
  email?: string ; // optional field
  contact: string;
};

// difference between type and interface 
// ? filed .. what is type inference, undefined , null
// what is a union in type script 
// what is called a record in typescript
// grid vs flex vs classical div vs tables 